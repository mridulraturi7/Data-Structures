/*
    C++ Corresponding Position in the two strings that hold exactly the same characters.
*/

#include<iostream>

using namespace std;

int main()
{
    int testCase;
    cin>>testCase;

    while(testCase-- != 0)
    {
        string s1, s2;
        cin>>s1>>s2;

        
    }

    return 0;
}